#' Tangram.pipe Iris Data
#'
#' This dataset is adapted from Edgar Anderson's Iris Data for use in the 
#' tangram.pipe package. Three new variables (stem size, leaf color, and flower color)
#' are introduced into the dataset as made-up categorical variables which were not 
#' from Fisher's original data. The dataset is expanded to 500 by sampling rows
#' from the original 150 row `iris` dataset.
#'
#' @docType data
#'
#' @usage data(iris2)
#'
#' @format `iris2` is a data frame with 500 rows sampled from the original `iris`
#' dataset. In addition, three made-up categorical variables are introduced to
#' the dataset, named `Stem.Size`, `Leaf.Color`, and `color`.
#'
#' @keywords datasets
#'
#' @source The `iris` dataset.
"iris2"